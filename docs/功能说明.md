# Dubai Property English Coach 功能说明

## 目标

给迪拜房产业务运营人员做一个碎片化英语学习工具。学习内容来自真实工作场景，不做普通背单词，重点帮助用户在句子、对话和任务里掌握英语。

## 知识库分类

知识库内部按业务类型组织，但学员学习时不手动切换分类，而是进入学习后按“上一个 / 下一个”连续学习。分类只作为当前知识点标签展示。

1. 线索沟通
   - TikTok 评论线索打招呼
   - 判断 buy / rent
   - 询问预算、区域、户型
   - 跟进未回复客户

2. Agent 协作
   - 约会议
   - 催房源视频和楼盘资料
   - 同步线索进展
   - 沟通看房、客户反馈、下一步安排

3. 新伙伴合作
   - 介绍我们的合作方式
   - 说明我们做线索收集和清洗，不承诺房源成交
   - 沟通分佣、独家线索、合作流程

4. 楼盘介绍
   - 介绍位置、户型、价格、付款计划
   - 说明投资和自住场景
   - 回答客户常见问题

5. 内容运营
   - 和 agent 沟通拍摄脚本、房源视频、户型图、价格和付款计划
   - 生成视频 caption 和发布前素材检查

6. 团队日常沟通
   - 同步今日进展、卡点、优先级
   - 跟进任务、确认下一步

7. WhatsApp / 客户跟进
   - 老线索轻量跟进
   - 客户忙、预算低、不信任、比较项目等常见回复

## 学习流程

第一版 MVP 有两个主模块，以及一个常驻 AI 老师：

1. 学习
   - 给出真实对话
   - 拆解核心句子
   - 提炼关键词
   - 增加常用句库
   - 读音不做独立模块：英文单词可点击朗读，每句话后有喇叭图标可读整句
   - 标出容易出错的表达
   - 页面按“案例对话、为什么这么说、关键词/句库、易错点、可直接用”分区，降低信息混乱
   - 不展示分类 tab；学员只需要顺序学习，可无限上一个 / 下一个
   - 学会后点击“标记学会了”，该知识点加入应用考试池
   - 学习库来源包含通用知识点和个人知识点
   - AI 老师生成的案例会加入个人学习库，后续也能被学习和检测

2. 检测
   - AI 扮演客户、agent 或合作伙伴
   - 基于用户已标记学会的知识点出题
   - 以 4 轮连续对话考试进行，不是一问一答
   - 用户每轮用英文回复
   - AI 会根据用户上一轮回复继续追问
   - 系统指出问题，并改写为更自然的业务英语
   - 重点检查是否表达清楚、是否礼貌、是否符合业务边界
   - 考试模式隐藏示范答案、关键词和常用句库，避免变成开卷考试
   - 考试结果会保存到学员档案，后续可用于复盘
   - 界面以模拟对话为主体，知识点只作为旁边 tips

3. 常驻 AI 老师
   - 用户粘贴日常工作中看到的英文句子、单词或自己的问题
   - 也可以直接输入中文，让 AI 生成自然英文表达
   - 不再区分“中文转英文 / 英文转中文”按钮，由老师自动判断该怎么处理
   - AI 先判断需求，再翻译、解释、润色或生成话术
   - 解释为什么这样说，哪些词适合当前业务场景
   - 自动生成一个贴近业务的学习案例
   - 解析结果可加入“我的学习库”，成为个人知识点
   - 同一个案例不能重复加入

4. 管理员用户管理
   - 管理员账号用于开通学员账号
   - 管理员只需要设置用户名和密码
   - 每个学员登录后拥有独立学习档案
   - 学习进度、考试记录、保存案例跟随账号保存，不只保存在单台电脑浏览器
   - 用户管理不和学习/检测并列，管理员从右上角独立入口进入

## 后续接入方向

第一阶段先做可点击原型和场景库。

当前已进入第一版 MVP 开发：
- 使用静态 Web 应用，打开 HTML 或本地服务即可运行
- 用 `localStorage` 和云端档案保存个人学习库、学习进度和考试记录
- 已加入网页端学习记录：每个场景可以标记学完，右侧显示已学场景、考试次数、保存案例数
- 已加入最近考试记录：完成多轮考试后自动保存答案和反馈摘要
- 已加入数据导出/导入：可把本地学习记录、考试记录和案例导出为 JSON 备份，换浏览器时再导入
- 已加入高频替换句：每个场景除了核心句和词汇，还提供可直接替换使用的工作句
- 已加入 Netlify Function AI 代理，前端不暴露 API key
- 没有配置模型 key 时，会自动使用本地规则兜底
- 数据结构已按学习、考试、AI 老师三条线整理
- 学习模块的单词点读和句子朗读使用浏览器朗读，不消耗 AI 模型额度
- 已加入 Cloudflare D1 用户系统：`users`、`sessions`、`user_state`
- 已加入管理员开账号页面：管理员可创建或重置学员账号
- 已加入服务端学习档案：登录用户的学习进度、考试记录、保存案例会同步到 D1
- 通用知识库已从 4 个样例扩充到 40+ 个工作知识点
- 通用知识库已新增 1000 条静态种子知识点，当前总量约 1041 条
- 1000 条种子知识点来源为人工设计的业务场景框架，覆盖线索、买房投资、租房、agent 协作、新伙伴合作、楼盘介绍、内容运营、团队日常、客户异议、social 日常英语
- 这批知识点不是网页后台 AI 生成，不消耗网站 gptsapi 额度
- AI 老师生成案例会加入个人学习库，不再作为孤立“我的案例”常驻卡片

## Token 消耗边界

不消耗 AI token：
- 登录、退出、管理员创建用户
- 学习页展示知识点、上一个 / 下一个
- 标记学会、保存学习进度
- 单词点读、句子朗读
- 查看学习库、导出 / 导入备份

会消耗 AI token：
- 检测里的动态多轮追问
- 检测结束后的对话分析和表达改写
- AI 老师问答、解析、中译英、英译中、生成个人学习知识点

后续资料库扩展：
- 学习资料库可以接入 Musewen 线上库，优先作为知识库来源同步到本地/数据库
- 同步后的学习资料仍按静态知识点展示，不在学员浏览学习页时临时调用 AI
- 冷启动阶段可以继续由 Codex 人工/半自动整理知识点写入静态库，不消耗网站 gptsapi 额度
- 后台知识库工厂上线后，再支持抓取、去重、AI 批量生成、审核发布

## 当前知识库来源

1. 早期手工 demo 场景
   - TikTok 线索沟通
   - agent 协作
   - 新伙伴合作
   - 楼盘介绍

2. 扩展工作场景
   - 线索筛选、租房、短租、WhatsApp 跟进
   - agent 视频、价格、看房、素材沟通
   - partner 分佣、独家、线索质量沟通
   - 内容运营、团队同步、客户异议处理

3. 1000 条静态种子知识点
   - 文件：`knowledge-base.js`
   - 来源标记：`manual-seed-library`
   - 生成方式：人工设计分类、场景、表达模板和业务边界后写入静态库
   - 消耗 token：不消耗网站 gptsapi 额度

## 当前网页端可用状态

- 可以作为账号制网页工具使用：登录、学习、检测、AI 老师、学习档案、单词点读和句子朗读都可在浏览器里完成。
- 当前 GitHub Pages 版本不运行后端函数，所以 AI 老师会走本地兜底案例；正式 AI 后端需要部署到支持函数的平台，如 Cloudflare Pages、Vercel 或恢复 Netlify。
- 学员记录已支持账号档案，正式线上以 Cloudflare D1 为准；本地浏览器仍保留备份能力。
- 如果不想把 gptsapi key 写入 Cloudflare，可以使用本地 AI 代理：运行 `npm run local` 后打开 `http://127.0.0.1:8788`，网页会调用本机 `/api/ai-teacher`，key 只留在本机 `.env.local` 或本地环境变量里。
- 当前本地 AI 代理已验证可用：`gptsapi / gpt-4.1-mini` 能返回真实学习案例，`fallback=false`。
- 正式多人使用方案：Cloudflare Pages + D1 登录账号。只有登录用户才能进入工具和调用 `/api/ai-teacher`，管理员可以给别人开账号。

## 模型选择规则

- 低风险、批量、学习类任务：优先使用 opencode 免费模型
  - 默认模型：`deepseek-v4-flash-free`
  - 环境变量：`OPENCODE_API_KEY`
  - 可选覆盖：`OPENCODE_BASE_URL`、`OPENCODE_FREE_MODEL`
- 需要更稳定质量或免费模型失败时：使用 gptsapi 付费模型兜底
  - 默认模型：`gpt-4.1-mini`
  - 环境变量：`GPTSAPI_API_KEY`
  - 兼容旧变量名：`GPTAPI_API_KEY`
  - 可选覆盖：`GPTAPI_BASE_URL`、`GPTAPI_QUALITY_MODEL`
- API key 只放 Cloudflare Secret / 平台环境变量，不写进前端代码和仓库。
- 当前 AI 老师、检测动态追问、检测反馈都走后端函数。
  - Netlify 路径：`/.netlify/functions/ai-teacher`
  - Vercel 路径：`/api/ai-teacher`
  - Cloudflare Pages 路径：`/api/ai-teacher`
  - 线上正式版优先部署到 Cloudflare Pages，因为它同时支持静态网页和后端函数。

## 线上预览

- Netlify 生产预览：https://incandescent-cucurucho-bece5d.netlify.app
- GitHub Pages 预览：https://cici-ok.github.io/dubai-property-english-coach/
- Cloudflare 正式预览：https://dubai-property-english-coach.pages.dev/
- 说明：Netlify 账号当前被 suspended，先改用 Cloudflare Pages 作为正式链接；GitHub Pages 不运行后端函数，AI 老师会自动使用本地兜底案例，单词点读和句子朗读正常可用。
- 说明：Cloudflare 版本已支持 `/api/ai-teacher` 后端函数，当前已配置 gptsapi Secret，线上 AI 老师和检测反馈可走真 AI。

第二阶段接入飞书机器人：
- 每天定时推送学习卡
- 支持用户直接回复练习
- 机器人给出修改建议和词汇解释

第三阶段接入 OpenClaw：
- 负责定时任务
- 负责长期运行机器人
- 后续可接微信或其他渠道
