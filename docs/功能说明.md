# Dubai Property English Coach 功能说明

## 目标

给迪拜房产业务运营人员做一个碎片化英语学习工具。学习内容来自真实工作场景，不做普通背单词，重点帮助用户在句子、对话和任务里掌握英语。

## 第一版模块

1. 线索沟通
   - TikTok 评论线索打招呼
   - 判断 buy / rent
   - 询问预算、区域、户型
   - 跟进未回复客户

2. Agent 协作
   - 约会议
   - 催房源视频和楼盘资料
   - 同步线索进展
   - 沟通看房、客户反馈、下一步安排

3. 新伙伴合作
   - 介绍我们的合作方式
   - 说明我们做线索收集和清洗，不承诺房源成交
   - 沟通分佣、独家线索、合作流程

4. 楼盘介绍
   - 介绍位置、户型、价格、付款计划
   - 说明投资和自住场景
   - 回答客户常见问题

## 学习流程

第一版 MVP 有三个主模块：

1. 学习
   - 给出真实对话
   - 拆解核心句子
   - 提炼关键词
   - 增加常用句库
   - 增加读音查询：展示重点句怎么拆开读、哪里重读，并可点击朗读
   - 标出容易出错的表达
   - 页面按“真实对话、核心表达、读音查询、关键词/句库、易错点、可直接用”分区，降低信息混乱

2. 应用考试
   - 机器人扮演客户、agent 或合作伙伴
   - 以 3-4 轮连续对话考试进行，不是一问一答
   - 用户每轮用英文回复
   - 系统指出问题，并改写为更自然的业务英语
   - 重点检查是否表达清楚、是否礼貌、是否符合业务边界
   - 考试模式隐藏示范答案、关键词和常用句库，避免变成开卷考试
   - 考试结果会保存到本地记录，后续可用于复盘

3. AI 老师解析
   - 用户粘贴日常工作中看到的英文句子、单词或自己的问题
   - 也可以直接输入中文，让 AI 生成自然英文表达
   - AI 先翻译和解释，再拆出重点词汇
   - 解释为什么这样说，哪些词适合当前业务场景
   - 自动生成一个贴近业务的学习案例
   - 解析结果可保存到“我的案例”，保存在浏览器本地

## 后续接入方向

第一阶段先做可点击原型和场景库。

当前已进入第一版 MVP 开发：
- 使用静态 Web 应用，打开 HTML 或本地服务即可运行
- 用 `localStorage` 保存“我的案例”和考试记录
- 已加入网页端学习记录：每个场景可以标记学完，右侧显示已学场景、考试次数、保存案例数
- 已加入最近考试记录：完成多轮考试后自动保存答案和反馈摘要
- 已加入数据导出/导入：可把本地学习记录、考试记录和案例导出为 JSON 备份，换浏览器时再导入
- 已加入高频替换句：每个场景除了核心句和词汇，还提供可直接替换使用的工作句
- 已加入 Netlify Function AI 代理，前端不暴露 API key
- 没有配置模型 key 时，会自动使用本地规则兜底
- 数据结构已按学习、考试、AI 老师三条线整理
- 学习模块的读音查询先使用本地课程数据和浏览器朗读，不消耗 AI 模型额度

## 当前网页端可用状态

- 可以作为个人网页工具使用：学习、考试、AI 老师解析、本地记录、读音查询都可在浏览器里完成。
- 当前 GitHub Pages 版本不运行后端函数，所以 AI 老师会走本地兜底案例；正式 AI 后端需要部署到支持函数的平台，如 Cloudflare Pages、Vercel 或恢复 Netlify。
- 记录保存在当前浏览器本地，不是云账号；正式云同步需要后续接数据库或账号系统。

## 模型选择规则

- 低风险、批量、学习类任务：优先使用 opencode 免费模型
  - 默认模型：`deepseek-v4-flash-free`
  - 环境变量：`OPENCODE_API_KEY`
  - 可选覆盖：`OPENCODE_BASE_URL`、`OPENCODE_FREE_MODEL`
- 需要更稳定质量或免费模型失败时：使用 gptsapi 付费模型兜底
  - 默认模型：`gpt-4.1-mini`
  - 环境变量：`GPTSAPI_API_KEY`
  - 兼容旧变量名：`GPTAPI_API_KEY`
  - 可选覆盖：`GPTAPI_BASE_URL`、`GPTAPI_QUALITY_MODEL`
- API key 只放 Netlify 环境变量，不写进前端代码和仓库。
- 当前 AI 老师解析、考试反馈都走后端函数。
  - Netlify 路径：`/.netlify/functions/ai-teacher`
  - Vercel 路径：`/api/ai-teacher`
  - Cloudflare Pages 路径：`/api/ai-teacher`
  - 线上正式版优先部署到 Cloudflare Pages，因为它同时支持静态网页和后端函数。

## 线上预览

- Netlify 生产预览：https://incandescent-cucurucho-bece5d.netlify.app
- GitHub Pages 预览：https://cici-ok.github.io/dubai-property-english-coach/
- 说明：Netlify 账号当前被 suspended，先改用 GitHub Pages 链接发给别人看；GitHub Pages 不运行后端函数，AI 老师会自动使用本地兜底案例，读音查询正常可用。

第二阶段接入飞书机器人：
- 每天定时推送学习卡
- 支持用户直接回复练习
- 机器人给出修改建议和词汇解释

第三阶段接入 OpenClaw：
- 负责定时任务
- 负责长期运行机器人
- 后续可接微信或其他渠道
